# Vimium Homemade

セキュリティ要件によりOSS拡張機能が使用できない環境向けに自作したVimium互換のChrome Extension。

**v1.2.0**

---

## インストール方法

1. Chrome で `chrome://extensions/` を開く
2. 右上の「デベロッパーモード」をONにする
3. 「パッケージ化されていない拡張機能を読み込む」をクリック
4. このリポジトリのルートディレクトリを選択する

---

## キーバインド一覧

### スクロール

| キー | 機能 |
|------|------|
| `j` / `k` | 下 / 上スクロール |
| `h` / `l` | 左 / 右スクロール |
| `d` / `u` | 半ページ下 / 上スクロール |
| `gg` | ページ最上部へ |
| `G` | ページ最下部へ |
| `zH` / `zL` | 横スクロールを左端 / 右端へ |

### ナビゲーション

| キー | 機能 |
|------|------|
| `H` / `L` | ブラウザ「戻る」 / 「進む」 |
| `r` | ページリロード |
| `gu` | URL階層を1段上へ |
| `gU` | URLをルートへ |
| `gs` | ページソースを表示 |

### タブ操作

| キー | 機能 |
|------|------|
| `J` / `K` | 左 / 右のタブへ |
| `gt` / `gT` | 右 / 左のタブへ |
| `g0` / `g$` | 最初 / 最後のタブへ |
| `t` | 新規タブ作成 |
| `x` | 現在タブを閉じる |
| `X` | 閉じたタブを復元 |
| `W` | 現在タブを新ウィンドウへ移動 |
| `T` | 開いているタブを検索して切り替え |
| `<<` / `>>` | タブを左 / 右へ移動 |

### Link Hints

| キー | 機能 |
|------|------|
| `f` | リンクヒント表示 → 現在タブで開く |
| `F` | リンクヒント表示 → 新規タブで開く |
| `yf` | リンクのURLをコピー |

### Vomnibar

| キー | 機能 |
|------|------|
| `o` | Vomnibar（現在タブ）— 履歴・ブックマーク統合検索 |
| `O` | Vomnibar（新規タブ） |
| `T` | タブ切り替え Vomnibar |

### 検索（Find Mode）

| キー | 機能 |
|------|------|
| `/` | Find Mode 開始 |
| `n` / `N` | 次 / 前の一致箇所へ |
| `Esc` | Find Mode 終了 |

### クリップボード

| キー | 機能 |
|------|------|
| `yy` | 現在ページのURLをコピー |
| `yf` | リンクのURLをコピー（Link Hints） |
| `yc` | 検索マッチを含むブロック要素のテキストをコピー（`/` 検索後に使用）|
| `p` / `P` | クリップボードのURLを現在タブ / 新規タブで開く |

### ビジュアルモード

| キー | 機能 |
|------|------|
| `v` | ビジュアルモード開始（`/` 検索マッチがあればそこから選択開始） |
| `V` | ビジュアルラインモード開始（行全体を選択） |

**ビジュアルモード内のキー：**

| キー | 機能 |
|------|------|
| `h` / `l` | 1文字左 / 右に選択拡張 |
| `w` / `b` | 1単語右 / 左に選択拡張 |
| `j` / `k` | 1行下 / 上に選択拡張 |
| `$` / `0` | 行末 / 行頭まで選択拡張 |
| `G` | ページ末まで選択拡張 |
| `v` / `V` | charモード ↔ lineモード切替 |
| `y` | 選択テキストをコピーしてノーマルモードへ |
| `Esc` | 選択解除してノーマルモードへ |

### その他

| キー | 機能 |
|------|------|
| `gi` | ページ最初の入力欄にフォーカス |
| `i` | Insert Mode（キーバインドを一時無効化） |
| `?` | ヘルプ表示（キーバインド一覧） |

---

## 主要機能の詳細

### Link Hints
`f` / `F` でページ上の可視リンク・ボタンにラベルを表示。ラベル文字を入力すると対象を開く。スクロールしないと見えない要素（カルーセル外など）は対象外。

### Find Mode
`/` で起動。スマートケース対応（小文字のみ → 大文字小文字を無視、大文字を含む → 区別）。`n`/`N` で移動。

### `yc` — ブロックテキストコピー
`/` で検索後に `yc` を押すと、マッチを含む最近傍のブロック要素（`<p>`, `<li>`, `<td>` 等）のテキスト全体をコピー。`<div>` のような汎用コンテナの場合はビジュアルモードに移行して手動で範囲を選択できる。

### Vomnibar
`o`/`O` で起動。URL・ブックマーク・履歴を統合検索。`T` でタブ切り替えモード。

### スクロール
Vimiumのスムーズスクロール実装を移植。長押しでの速度キャリブレーション機能付き。テキスト入力中（`input`/`textarea`/`contenteditable`）はキーバインドを自動無効化。

---

## 設定

`chrome://extensions/` → Vimium Homemade の「詳細」→「拡張機能のオプション」から設定ページを開ける。

| 設定項目 | 内容 |
|----------|------|
| スクロール量 | 1回のスクロールのpx数 |
| スムーズスクロール | ON/OFF |
| 半ページ比率 | `d`/`u` のスクロール量（画面高さの割合） |
| キーリピート | 長押し時のディレイ・インターバル |
| ヒント文字 | Link Hints に使用する文字セット |
| Vomnibar | 最大表示件数・履歴/ブックマークの有効無効 |
| HUDタイムアウト | メッセージの表示時間 |
| キーマッピング | カスタムキーバインド（`map`/`unmap`/`unmapAll` 形式） |
| 除外サイト | キーバインドを無効化するドメイン（ワイルドカード対応） |

---

## 制限事項

- `chrome://` URL では動作不可（ブラウザセキュリティポリシー）
- Chrome Web Store ページでは動作不可
- PDFビューアーでは動作不可
- `file://` は拡張機能の設定で手動許可が必要
