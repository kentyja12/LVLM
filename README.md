# Vimium Homemade

セキュリティ要件によりOSS拡張機能が使用できない環境向けに自作したVimium互換のChrome Extension。

---

## インストール方法

1. Chrome で `chrome://extensions/` を開く
2. 右上の「デベロッパーモード」をONにする
3. 「パッケージ化されていない拡張機能を読み込む」をクリック
4. このリポジトリのルートディレクトリを選択する

---

## キーバインド一覧

### ページ操作系

| キー | 機能 |
|------|------|
| `j` | 下スクロール |
| `k` | 上スクロール |
| `h` | 左スクロール |
| `l` | 右スクロール |
| `d` | 半ページ下スクロール |
| `u` | 半ページ上スクロール |
| `gg` | ページ最上部へ |
| `G` | ページ最下部へ |
| `r` | ページリロード |

### リンク操作系（Link Hints）

| キー | 機能 |
|------|------|
| `f` | リンクヒント表示 → 現在タブで開く |
| `F` | リンクヒント表示 → 新規タブで開く |
| `yf` | リンクのURLをコピー |

### タブ操作系

| キー | 機能 |
|------|------|
| `J` | 左のタブへ |
| `K` | 右のタブへ |
| `t` | 新規タブ作成 |
| `x` | 現在タブを閉じる |
| `X` | 閉じたタブを復元 |

### ナビゲーション系

| キー | 機能 |
|------|------|
| `H` | ブラウザ「戻る」 |
| `L` | ブラウザ「進む」 |
| `gu` | URL階層を1段上へ |
| `gU` | URLをルートへ |
| `o` | Vomnibar（現在タブ） |
| `O` | Vomnibar（新規タブ） |

### 検索系（Find Mode）

| キー | 機能 |
|------|------|
| `/` | Find Mode開始 |
| `n` | 次の一致へ |
| `N` | 前の一致へ |
| `Esc` | Find Mode終了 |

### ユーティリティ

| キー | 機能 |
|------|------|
| `i` | Insert Mode（Vimium一時無効化） |
| `?` | ヘルプ表示 |

---

## 特殊機能

### Link Hints
`f` または `F` を押すとページ上の全リンク・ボタンに2文字のラベルが表示される。ラベルを入力すると対象を開く。

### Find Mode
`/` で起動。スマートケース対応（小文字のみ入力で大文字小文字を無視、大文字を含むと区別）。`n`/`N` で前後の一致箇所に移動。

### Insert Mode
`i` で起動。Vimiumのキーバインドを一時的に無効化し、ページのネイティブ入力を優先する。`Esc` で終了。

### Vomnibar
`o`/`O` で起動するコマンドパレット。URL・ブックマーク・履歴を統合検索できる。

---

## 設定

`chrome://extensions/` → Vimium Homemade の「詳細」→「拡張機能のオプション」から設定ページを開ける。

- キーバインドのカスタマイズ（未実装）
- サイト除外設定（未実装）

---

## 制限事項

- `chrome://` URL では動作不可（ブラウザセキュリティポリシー）
- Chrome Web Store ページでは動作不可
- PDFビューアーでは動作不可
- `file://` は拡張機能の設定で手動許可が必要
